EXTRACTOR_SHARED_SECRET=change-me
# Only needed for docker-compose.synology.yml (Tailscale Funnel sidecar).
# Generate at https://login.tailscale.com/admin/settings/keys — use a
# reusable, non-ephemeral key so the container can re-auth after restarts.
TS_AUTHKEY=change-me
