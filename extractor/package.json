{
  "name": "videojam-extractor",
  "version": "1.0.0",
  "private": true,
  "description": "Standalone always-on service that resolves a YouTube video ID to a playable stream URL for the tvOS app, using youtubei.js against a self-hosted PO-token provider instead of scraping public Piped/Invidious instances.",
  "type": "module",
  "engines": {
    "node": ">=20"
  },
  "scripts": {
    "build": "tsc -p tsconfig.json",
    "start": "node dist/server.js",
    "dev": "tsx watch src/server.ts"
  },
  "dependencies": {
    "youtubei.js": "^18.0.0"
  },
  "devDependencies": {
    "@types/node": "^20.14.12",
    "tsx": "^4.19.2",
    "typescript": "^5.6.2"
  }
}
